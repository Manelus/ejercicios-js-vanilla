const i = prompt("¿Cuántos tipos de artículo?");
		
		for (let i = 1; i <= tipos; i++) {
			let total= promp("¿Cuántos artículos del tipo " + i + "?");
			total += 0;
		}

		console.log("Total artículos : " + total);

